{
  "name": "js-mn-istanbuljs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "e2e": "jest",
    "start": "react-scripts start",
    "build": "react-scripts build && cpx package.json build && cpx package-lock.json build && cpx e2e/**/*.js build/e2e && cpx jest.config.js build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "nyc": "^14.1.1",
    "puppeteer": "^1.18.0",
    "remap-istanbul": "^0.13.0",
    "serve": "^11.0.2"
  }
}
